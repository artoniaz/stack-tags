import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: "light",
  },
  typography: {
    fontFamily: ["Lato", "sans-serif"].join(","),
    fontSize: 14,
  },
  
});
